# — Rust build artifacts —
target/                   # compiled binaries & intermediate files
**/*.rs.bk                # any editor backups of your source

# — OS / editor cruft —
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~                       # Emacs backup files
#*#                      # more Emacs temp files

# — dotenv & local secrets —
.env                     # local env vars (API keys, DB URLs, etc.)

# — Private config JSONs (real metadata) —
config/tables.json
config/table_columns/*.json

# — Share only example/templates — 
!config/tables.example.json
!config/table_columns/*.example.json

# — Local databases & scratch data —
*.sqlite

# — CSV data — 
# ignore all CSVs by default, but keep our sample example
data/**/*.csv
!data/examples/sample.csv

# — Logs & reports —
*.log
reports/
